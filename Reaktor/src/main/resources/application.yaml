spring:
  jpa.hibernate.ddl-auto: none
  datasource:
    url: jdbc:mysql://localhost:3306/reaktor
    username: root
    password: root
    hikari:
      connection-timeout: 60000
      maximum-pool-size: 5
server:
  port: 8084
